<UserControl x:Class="SimpleCoinTrading.Wpf.Sample2"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SimpleCoinTrading.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             d:DesignInstance="{d:DesignInstance local:Sample2ViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="200"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel 
            Orientation="Horizontal" HorizontalAlignment="Left" Margin="4,10,0,10">
            <CheckBox x:Name="chkKillSwitch" Content="KillSwitch"
                      IsChecked="{Binding KillSwitchEnabled}"
                      IsEnabled="False"
                      Margin="12,0,0,0"/>
            <Button Content="Toggle"
                    Command="{Binding ToggleKillSwitchCommand}"
                    Width="80" Margin="8,0,0,0"/>
        </StackPanel>
        <local:Algorithm Grid.Row="1" Grid.Column="0" DataContext="{Binding AlgorithmViewModel}"/>

        <local:Position Grid.Row="1" Grid.Column="1" 
                        DataContext="{Binding PositionsViewModel}"/>
        <local:Orders Grid.Row="2" Grid.Column="0"
                      Grid.ColumnSpan="2"
                      DataContext="{Binding OrdersViewModel}"/>
        
        <local:Logs Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" DataContext="{Binding LogViewModel}"/>
    </Grid>
</UserControl>
