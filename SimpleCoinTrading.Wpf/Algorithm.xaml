<UserControl x:Class="SimpleCoinTrading.Wpf.Algorithm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SimpleCoinTrading.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="400"
             d:DesignInstance="{d:DesignInstance local:AlgorithmViewModel}">
    <Grid>
        <DataGrid Margin="4" ItemsSource="{Binding Algorithms}" SelectedItem="{Binding SelectedAlgorithm}" AutoGenerateColumns="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="AlgoId" 
                                    Binding="{Binding Name}" 
                                    Width="150"/>
                <DataGridTextColumn Header="Status" 
                                    Binding="{Binding Status}"
                                    Width="80"/>
                <DataGridTextColumn Header="Message" 
                                    Binding="{Binding Message}" 
                                    Width="*">
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
